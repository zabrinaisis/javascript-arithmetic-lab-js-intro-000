function add(x, y) {
  return (x + y)
}

function subtract(x, y) {
  return (y - x)
}

function multiply(x, y) {
  return (x * y)
}

function divide(x, y) {
  return (x / y)
}

function increment(x) {
  return (x += 1)
}

function decrement(x) {
  return (x -= 1)
}
